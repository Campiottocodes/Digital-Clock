const size = {
  usm: "400px",
  sm: "450px",
  md: "550px",
  ml: "650px",
  bg: "700px",
  xm: "850px",
  xl: "1050px",
};

const breakpoints = {
  usm: `(max-width: ${size.usm})`,
  sm: `(max-width: ${size.sm})`,
  md: `(max-width: ${size.md})`,
  ml: `(max-width: ${size.ml})`,
  bg: `(max-width: ${size.bg})`,
  xm: `(max-width: ${size.xm})`,
  xl: `(max-width: ${size.xl})`,
};

export default breakpoints;
